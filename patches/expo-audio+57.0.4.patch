diff --git a/node_modules/expo-audio/ios/AudioModule.swift b/node_modules/expo-audio/ios/AudioModule.swift
index d265606..7fce9ee 100644
--- a/node_modules/expo-audio/ios/AudioModule.swift
+++ b/node_modules/expo-audio/ios/AudioModule.swift
@@ -597,7 +597,10 @@ public class AudioModule: Module {
         case .duckOthers:
           playerVolumes[playable.id] = playable.volume
           playable.volume *= 0.5
-        case .doNotMix, .mixWithOthers:
+        case .mixWithOthers:
+          // In mixWithOthers mode, do not pause. Keep playing alongside other apps and background refreshes.
+          break
+        case .doNotMix:
           playable.pause()
         }
       }
@@ -615,12 +618,10 @@ public class AudioModule: Module {
   private func handleInterruptionEnded(with options: AVAudioSession.InterruptionOptions) {
     do {
       try AVAudioSession.sharedInstance().setActive(true)
-      if options.contains(.shouldResume) {
-        resumeInterruptedPlayers()
-      }
     } catch {
       print("Failed to reactivate audio session: \(error)")
     }
+    resumeInterruptedPlayers()
   }
 
   @objc private func handleAudioSessionRouteChange(_ notification: Notification) {
@@ -633,6 +634,12 @@ public class AudioModule: Module {
     switch reason {
     case .oldDeviceUnavailable:
       pauseAllPlayers()
+    case .newDeviceAvailable:
+      registry.allPlayers.values.forEach { player in
+        if player.isPlaying || player.wasPlaying {
+          player.resumePlayback()
+        }
+      }
     default:
       break
     }
@@ -808,6 +815,8 @@ public class AudioModule: Module {
 #else
         categoryOptions.insert(.allowBluetooth)
 #endif
+      } else if category == .playback {
+        categoryOptions.insert([.allowBluetoothA2DP, .allowAirPlay])
       }
 #endif
 
diff --git a/node_modules/expo-audio/ios/AudioPlayer.swift b/node_modules/expo-audio/ios/AudioPlayer.swift
index 0742943..f229d6a 100644
--- a/node_modules/expo-audio/ios/AudioPlayer.swift
+++ b/node_modules/expo-audio/ios/AudioPlayer.swift
@@ -24,6 +24,8 @@ public class AudioPlayer: SharedRef<AVPlayer>, Playable {
   }
   var samplingEnabled = false
   var keepAudioSessionActive = false
+  private var playerLooper: AVPlayerLooper?
+  private var looperStatusObserver: NSKeyValueObservation?
 
   var isLooping = false {
     didSet {
@@ -31,16 +33,46 @@ public class AudioPlayer: SharedRef<AVPlayer>, Playable {
         return
       }
       if isLooping {
-        ref.actionAtItemEnd = .advance
-        enqueueNextLoopItem()
+        setupLooper()
+        if ref.timeControlStatus == .playing {
+          ref.playImmediately(atRate: effectiveRate)
+        }
       } else {
-        removeQueuedLoopItems()
+        looperStatusObserver = nil
+        playerLooper?.disableLooping()
+        playerLooper = nil
         ref.actionAtItemEnd = .pause
       }
       updateStatus(with: [:])
     }
   }
 
+  private func setupLooper() {
+    looperStatusObserver = nil
+    playerLooper?.disableLooping()
+    playerLooper = nil
+
+    guard isLooping,
+          let queuePlayer = ref as? AVQueuePlayer,
+          let source = self.source,
+          let templateItem = AudioUtils.createAVPlayerItem(from: source) else {
+      return
+    }
+
+    queuePlayer.removeAllItems()
+    let looper = AVPlayerLooper(player: queuePlayer, templateItem: templateItem)
+    self.playerLooper = looper
+
+    looperStatusObserver = looper.observe(\.status, options: [.initial, .new]) { [weak self, weak queuePlayer] l, _ in
+      guard let self, let queuePlayer else { return }
+      if l.status == .ready {
+        if self.ref.timeControlStatus == .playing || self.wasPlaying {
+          queuePlayer.playImmediately(atRate: self.effectiveRate)
+        }
+      }
+    }
+  }
+
   private var source: AudioSource?
 
   // MARK: Observers
@@ -95,14 +127,16 @@ public class AudioPlayer: SharedRef<AVPlayer>, Playable {
     ref.isBuffering(isPlaying: isPlaying)
   }
 
-  private var effectiveRate: Float {
+  var effectiveRate: Float {
     currentRate > 0 ? currentRate : 1.0
   }
 
   func play(at rate: Float) {
-    ref.actionAtItemEnd = isLooping ? .advance : .pause
-    if isLooping {
-      enqueueNextLoopItem()
+    if isLooping && playerLooper == nil {
+      setupLooper()
+    }
+    if playerLooper == nil {
+      ref.actionAtItemEnd = isLooping ? .advance : .pause
     }
     addPlaybackEndNotification()
     registerTimeObserver()
@@ -207,8 +241,8 @@ public class AudioPlayer: SharedRef<AVPlayer>, Playable {
           self.updateStatus(with: [
             "isLoaded": true
           ])
-          if self.isLooping {
-            self.enqueueNextLoopItem()
+          if self.playerLooper == nil {
+            self.ref.actionAtItemEnd = self.isLooping ? .advance : .pause
           }
           if shouldInstallAudioTap || samplingEnabled {
             installTap()
@@ -228,10 +262,10 @@ public class AudioPlayer: SharedRef<AVPlayer>, Playable {
         guard let self else {
           return
         }
-        if self.isLooping {
-          self.enqueueNextLoopItem()
-          self.addPlaybackEndNotification()
+        if self.playerLooper == nil {
+          self.ref.actionAtItemEnd = self.isLooping ? .advance : .pause
         }
+        self.addPlaybackEndNotification()
         if self.samplingEnabled && self.isLoaded {
           self.uninstallTap()
           self.installTap()
@@ -264,6 +298,8 @@ public class AudioPlayer: SharedRef<AVPlayer>, Playable {
     self.source = source
     let wasPlaying = ref.timeControlStatus == .playing
     let wasSamplingEnabled = samplingEnabled
+    playerLooper?.disableLooping()
+    playerLooper = nil
     removeQueuedLoopItems()
     ref.pause()
 
@@ -319,6 +355,9 @@ public class AudioPlayer: SharedRef<AVPlayer>, Playable {
   }
 
   private func teardownPlayer() {
+    looperStatusObserver = nil
+    playerLooper?.disableLooping()
+    playerLooper = nil
     removeQueuedLoopItems()
     if samplingEnabled {
       uninstallTap()
@@ -405,6 +444,9 @@ public class AudioPlayer: SharedRef<AVPlayer>, Playable {
   }
 
   private func replacePlayerItem(with item: AVPlayerItem?) {
+    looperStatusObserver = nil
+    playerLooper?.disableLooping()
+    playerLooper = nil
     if let queuePlayer = ref as? AVQueuePlayer {
       queuePlayer.removeAllItems()
       if let item {
@@ -413,6 +455,9 @@ public class AudioPlayer: SharedRef<AVPlayer>, Playable {
     } else {
       ref.replaceCurrentItem(with: item)
     }
+    if isLooping {
+      setupLooper()
+    }
   }
 
   private func enqueueNextLoopItem() {
@@ -449,24 +494,30 @@ public class AudioPlayer: SharedRef<AVPlayer>, Playable {
       object: nil,
       queue: nil
     ) { [weak self] notification in
-      guard let self,
-        let finishedItem = notification.object as? AVPlayerItem else {
-        return
-      }
-
-      guard let queuePlayer = self.ref as? AVQueuePlayer,
-        finishedItem == queuePlayer.currentItem || queuePlayer.items().contains(finishedItem) else {
+      guard let self else {
         return
       }
 
       if !self.isLooping {
-        let currentTime = finishedItem.duration.seconds
+        let finishedItem = notification.object as? AVPlayerItem
+        let currentTime = finishedItem?.duration.seconds ?? 0.0
         self.updateStatus(with: [
           "playing": false,
           "currentTime": currentTime.isNaN ? 0.0 : currentTime,
           "didJustFinish": true
         ])
         self.onPlaybackComplete?()
+      } else {
+        // When looping with AVPlayerLooper, the queue player transitions seamlessly
+        // at the hardware level. No seeking or manual restarts needed.
+        if self.playerLooper == nil {
+          if let currentItem = self.ref.currentItem {
+            currentItem.seek(to: .zero, toleranceBefore: .zero, toleranceAfter: .zero) { [weak self] _ in
+              guard let self else { return }
+              self.ref.playImmediately(atRate: self.effectiveRate)
+            }
+          }
+        }
       }
     }
   }
@@ -497,13 +548,30 @@ public class AudioPlayer: SharedRef<AVPlayer>, Playable {
 
   func pause() {
     ref.pause()
+    if isActiveForLockScreen {
+      MediaController.shared.updateNowPlayingInfo(for: self)
+    }
+    self.emit(event: AudioConstants.playbackStatus, payload: [
+      "remotePause": true,
+      "playing": false
+    ])
   }
 
   func resumePlayback() {
-    ref.play()
+    ref.playImmediately(atRate: effectiveRate)
+    if isActiveForLockScreen {
+      MediaController.shared.updateNowPlayingInfo(for: self)
+    }
+    self.emit(event: AudioConstants.playbackStatus, payload: [
+      "remotePlay": true,
+      "playing": true
+    ])
   }
 
   public override func sharedObjectWillRelease() {
+    looperStatusObserver = nil
+    playerLooper?.disableLooping()
+    playerLooper = nil
     ref.currentItem?.cancelPendingSeeks()
     owningRegistry?.remove(self)
 
diff --git a/node_modules/expo-audio/ios/MediaController.swift b/node_modules/expo-audio/ios/MediaController.swift
index e977f57..8ef056f 100644
--- a/node_modules/expo-audio/ios/MediaController.swift
+++ b/node_modules/expo-audio/ios/MediaController.swift
@@ -14,6 +14,7 @@ class MediaController {
   private var artworkLoadToken: UUID?
   private var currentArtworkItemIdentifier: ObjectIdentifier?
   private var isLiveStream = false
+  private var areRemoteCommandsConfigured = false
 
   func setActivePlayer(_ player: AudioPlayer?, options: LockScreenOptions? = nil) {
     performOnMain {
@@ -67,10 +68,18 @@ class MediaController {
       applyArtwork(&nowPlayingInfo, from: userMetadata, for: player, currentItemIdentifier: nextArtworkItemIdentifier)
     } else {
       resetArtworkState()
-      nowPlayingInfo.removeValue(forKey: MPMediaItemPropertyArtwork)
+      if let defaultArtwork = defaultAppArtwork() {
+        nowPlayingInfo[MPMediaItemPropertyArtwork] = defaultArtwork
+      } else {
+        nowPlayingInfo.removeValue(forKey: MPMediaItemPropertyArtwork)
+      }
       applyAssetMetadata(&nowPlayingInfo, for: player)
     }
 
+    if nowPlayingInfo[MPMediaItemPropertyArtwork] == nil {
+      nowPlayingInfo[MPMediaItemPropertyArtwork] = defaultAppArtwork()
+    }
+
     nowPlayingInfoCenter.nowPlayingInfo = nowPlayingInfo
   }
 
@@ -80,10 +89,12 @@ class MediaController {
       info.removeValue(forKey: MPMediaItemPropertyPlaybackDuration)
       info.removeValue(forKey: MPNowPlayingInfoPropertyElapsedPlaybackTime)
     } else {
-      info[MPMediaItemPropertyPlaybackDuration] = player.duration
+      let duration = player.duration > 0 && !player.duration.isNaN ? player.duration : 3600.0
+      info[MPMediaItemPropertyPlaybackDuration] = duration
       info[MPNowPlayingInfoPropertyElapsedPlaybackTime] = player.currentTime
     }
-    info[MPNowPlayingInfoPropertyPlaybackRate] = player.isPlaying ? player.ref.rate : 0.0
+    let isRateActive = player.isPlaying || player.ref.rate > 0 || player.ref.timeControlStatus != .paused
+    info[MPNowPlayingInfoPropertyPlaybackRate] = isRateActive ? 1.0 : 0.0
     info[MPNowPlayingInfoPropertyMediaType] = MPNowPlayingInfoMediaType.audio.rawValue
   }
 
@@ -130,6 +141,53 @@ class MediaController {
     }
   }
 
+  private func defaultAppArtwork() -> MPMediaItemArtwork? {
+    let mainBundle = Bundle.main
+    var image: UIImage? = UIImage(named: "NowPlayingLogo", in: mainBundle, compatibleWith: nil)
+      ?? UIImage(named: "AppLogo", in: mainBundle, compatibleWith: nil)
+      ?? UIImage(named: "NowPlayingLogo")
+      ?? UIImage(named: "AppLogo")
+
+    if image == nil {
+      if let path = mainBundle.path(forResource: "NowPlayingLogo", ofType: "png") ?? mainBundle.path(forResource: "artwork-logo-black", ofType: "png") {
+        image = UIImage(contentsOfFile: path)
+      }
+    }
+
+    if image == nil {
+      image = generateCalmpalLogo()
+    }
+
+    guard let finalImage = image else {
+      return nil
+    }
+
+    let boundsSize = finalImage.size.width > 0 ? finalImage.size : CGSize(width: 512, height: 512)
+    return MPMediaItemArtwork(boundsSize: boundsSize) { _ in finalImage }
+  }
+
+  private func generateCalmpalLogo() -> UIImage {
+    let size = CGSize(width: 512, height: 512)
+    UIGraphicsBeginImageContextWithOptions(size, true, 0.0)
+    guard let ctx = UIGraphicsGetCurrentContext() else {
+      UIGraphicsEndImageContext()
+      return UIImage()
+    }
+    ctx.setFillColor(UIColor.black.cgColor)
+    ctx.fill(CGRect(origin: .zero, size: size))
+    ctx.setStrokeColor(UIColor.white.cgColor)
+    ctx.setLineWidth(10)
+    ctx.setLineCap(.round)
+    let center = CGPoint(x: 256, y: 256)
+    let path = UIBezierPath(arcCenter: center, radius: 110, startAngle: 0, endAngle: .pi * 2, clockwise: true)
+    path.lineWidth = 10
+    UIColor.white.setStroke()
+    path.stroke()
+    let rendered = UIGraphicsGetImageFromCurrentImageContext() ?? UIImage()
+    UIGraphicsEndImageContext()
+    return rendered
+  }
+
   private func applyArtwork(
     _ info: inout [String: Any],
     from metadata: Metadata,
@@ -138,7 +196,11 @@ class MediaController {
   ) {
     guard let artworkUrl = metadata.artworkUrl else {
       resetArtworkState()
-      info.removeValue(forKey: MPMediaItemPropertyArtwork)
+      if let defaultArtwork = defaultAppArtwork() {
+        info[MPMediaItemPropertyArtwork] = defaultArtwork
+      } else {
+        info.removeValue(forKey: MPMediaItemPropertyArtwork)
+      }
       return
     }
 
@@ -173,14 +235,16 @@ class MediaController {
         return
       }
 
-      guard let artwork else {
+      let finalArtwork = artwork ?? self.defaultAppArtwork()
+
+      guard let finalArtwork else {
         return
       }
 
-      self.cachedArtwork = artwork
+      self.cachedArtwork = finalArtwork
 
       var latestNowPlayingInfo = self.nowPlayingInfoCenter.nowPlayingInfo ?? [String: Any]()
-      latestNowPlayingInfo[MPMediaItemPropertyArtwork] = artwork
+      latestNowPlayingInfo[MPMediaItemPropertyArtwork] = finalArtwork
       self.nowPlayingInfoCenter.nowPlayingInfo = latestNowPlayingInfo
     }
   }
@@ -208,6 +272,16 @@ class MediaController {
   }
 
   private func loadArtworkFromURL(url: URL, completion: @escaping (MPMediaItemArtwork?) -> Void) {
+    if url.isFileURL {
+      if let image = UIImage(contentsOfFile: url.path) {
+        let artwork = MPMediaItemArtwork(boundsSize: image.size) { _ in image }
+        self.performOnMain {
+          completion(artwork)
+        }
+        return
+      }
+    }
+
     URLSession.shared.dataTask(with: url) { data, _, error in
       if error != nil {
         self.performOnMain {
@@ -232,80 +306,113 @@ class MediaController {
   }
 
   private func enableRemoteCommands(options: LockScreenOptions?) {
-    remoteCommandCenter.playCommand.addTarget { [weak self] _ in
-      guard let player = self?.activePlayer else {
-        return .commandFailed
-      }
+    if !areRemoteCommandsConfigured {
+      areRemoteCommandsConfigured = true
 
-      player.play(at: Float(player.currentRate > 0 ? player.currentRate : 1.0))
-      return .success
-    }
+      remoteCommandCenter.playCommand.addTarget { [weak self] _ in
+        guard let self = self, let player = self.activePlayer else {
+          return .commandFailed
+        }
 
-    remoteCommandCenter.pauseCommand.addTarget { [weak self] _ in
-      guard let player = self?.activePlayer else {
-        return .commandFailed
+        let rate: Float = player.currentRate > 0 ? player.currentRate : 1.0
+        player.play(at: rate)
+        self.updateNowPlayingInfo(for: player)
+        player.emit(event: "playbackStatusUpdate", payload: ["remotePlay": true, "playing": true])
+        return .success
       }
 
-      player.ref.pause()
-      return .success
-    }
+      remoteCommandCenter.pauseCommand.addTarget { [weak self] _ in
+        guard let self = self, let player = self.activePlayer else {
+          return .commandFailed
+        }
 
-    remoteCommandCenter.togglePlayPauseCommand.addTarget { [weak self] _ in
-      guard let player = self?.activePlayer else {
-        return .commandFailed
+        player.pause()
+        self.updateNowPlayingInfo(for: player)
+        player.emit(event: "playbackStatusUpdate", payload: ["remotePause": true, "playing": false])
+        return .success
       }
 
-      if player.isPlaying {
-        player.ref.pause()
-      } else {
-        player.play(at: Float(player.currentRate > 0 ? player.currentRate : 1.0))
+      remoteCommandCenter.togglePlayPauseCommand.addTarget { [weak self] _ in
+        guard let self = self, let player = self.activePlayer else {
+          return .commandFailed
+        }
+
+        let isCurrentlyPlaying = player.isPlaying || player.ref.rate > 0 || player.ref.timeControlStatus != .paused
+        if isCurrentlyPlaying {
+          player.pause()
+          self.updateNowPlayingInfo(for: player)
+          player.emit(event: "playbackStatusUpdate", payload: ["remotePause": true, "playing": false])
+        } else {
+          let rate: Float = player.currentRate > 0 ? player.currentRate : 1.0
+          player.play(at: rate)
+          self.updateNowPlayingInfo(for: player)
+          player.emit(event: "playbackStatusUpdate", payload: ["remotePlay": true, "playing": true])
+        }
+        return .success
       }
-      return .success
-    }
 
-    remoteCommandCenter.changePlaybackPositionCommand.addTarget { [weak self] event in
-      guard let player = self?.activePlayer,
-      let event = event as? MPChangePlaybackPositionCommandEvent else {
-        return .commandFailed
+      remoteCommandCenter.changePlaybackPositionCommand.addTarget { [weak self] event in
+        guard let player = self?.activePlayer,
+        let event = event as? MPChangePlaybackPositionCommandEvent else {
+          return .commandFailed
+        }
+
+        let seekTime = CMTime(seconds: event.positionTime, preferredTimescale: 1)
+        player.ref.seek(to: seekTime)
+
+        return .success
       }
 
-      let seekTime = CMTime(seconds: event.positionTime, preferredTimescale: 1)
-      player.ref.seek(to: seekTime)
+      remoteCommandCenter.skipForwardCommand.preferredIntervals = [10.0]
+      remoteCommandCenter.skipForwardCommand.addTarget { [weak self] event in
+        guard let player = self?.activePlayer,
+        let event = event as? MPSkipIntervalCommandEvent else {
+          return .commandFailed
+        }
 
-      return .success
-    }
+        let currentTime = player.ref.currentTime()
+        let seekTime = currentTime + CMTime(seconds: event.interval, preferredTimescale: 1)
+        player.ref.seek(to: seekTime, toleranceBefore: .zero, toleranceAfter: .zero)
 
-    remoteCommandCenter.skipForwardCommand.preferredIntervals = [10.0]
-    remoteCommandCenter.skipForwardCommand.addTarget { [weak self] event in
-      guard let player = self?.activePlayer,
-      let event = event as? MPSkipIntervalCommandEvent else {
-        return .commandFailed
+        return .success
       }
 
-      let currentTime = player.ref.currentTime()
-      let seekTime = currentTime + CMTime(seconds: event.interval, preferredTimescale: 1)
-      player.ref.seek(to: seekTime, toleranceBefore: .zero, toleranceAfter: .zero)
+      remoteCommandCenter.skipBackwardCommand.preferredIntervals = [10.0]
+      remoteCommandCenter.skipBackwardCommand.addTarget { [weak self] event in
+        guard let player = self?.activePlayer,
+        let event = event as? MPSkipIntervalCommandEvent else {
+          return .commandFailed
+        }
 
-      return .success
-    }
+        let currentTime = player.ref.currentTime()
+        let seekTime = currentTime - CMTime(seconds: event.interval, preferredTimescale: 1)
+        player.ref.seek(to: seekTime, toleranceBefore: .zero, toleranceAfter: .zero)
 
-    remoteCommandCenter.skipBackwardCommand.preferredIntervals = [10.0]
-    remoteCommandCenter.skipBackwardCommand.addTarget { [weak self] event in
-      guard let player = self?.activePlayer,
-      let event = event as? MPSkipIntervalCommandEvent else {
-        return .commandFailed
+        return .success
       }
 
-      let currentTime = player.ref.currentTime()
-      let seekTime = currentTime - CMTime(seconds: event.interval, preferredTimescale: 1)
-      player.ref.seek(to: seekTime, toleranceBefore: .zero, toleranceAfter: .zero)
+      remoteCommandCenter.nextTrackCommand.addTarget { [weak self] _ in
+        guard let player = self?.activePlayer else {
+          return .commandFailed
+        }
+        player.emit(event: "playbackStatusUpdate", payload: ["remoteNext": true])
+        return .success
+      }
 
-      return .success
+      remoteCommandCenter.previousTrackCommand.addTarget { [weak self] _ in
+        guard let player = self?.activePlayer else {
+          return .commandFailed
+        }
+        player.emit(event: "playbackStatusUpdate", payload: ["remotePrevious": true])
+        return .success
+      }
     }
 
     remoteCommandCenter.playCommand.isEnabled = true
     remoteCommandCenter.pauseCommand.isEnabled = true
     remoteCommandCenter.togglePlayPauseCommand.isEnabled = true
+    remoteCommandCenter.nextTrackCommand.isEnabled = true
+    remoteCommandCenter.previousTrackCommand.isEnabled = true
     remoteCommandCenter.changePlaybackPositionCommand.isEnabled = !isLiveStream
     remoteCommandCenter.skipForwardCommand.isEnabled = options?.showSeekForward ?? false
     remoteCommandCenter.skipBackwardCommand.isEnabled = options?.showSeekBackward ?? false
@@ -315,16 +422,10 @@ class MediaController {
     remoteCommandCenter.playCommand.isEnabled = false
     remoteCommandCenter.pauseCommand.isEnabled = false
     remoteCommandCenter.togglePlayPauseCommand.isEnabled = false
+    remoteCommandCenter.nextTrackCommand.isEnabled = false
+    remoteCommandCenter.previousTrackCommand.isEnabled = false
     remoteCommandCenter.changePlaybackPositionCommand.isEnabled = false
     remoteCommandCenter.skipForwardCommand.isEnabled = false
     remoteCommandCenter.skipBackwardCommand.isEnabled = false
-
-    // Remove event targets
-    remoteCommandCenter.playCommand.removeTarget(self)
-    remoteCommandCenter.pauseCommand.removeTarget(self)
-    remoteCommandCenter.togglePlayPauseCommand.removeTarget(self)
-    remoteCommandCenter.changePlaybackPositionCommand.removeTarget(self)
-    remoteCommandCenter.skipForwardCommand.removeTarget(self)
-    remoteCommandCenter.skipBackwardCommand.removeTarget(self)
   }
 }
